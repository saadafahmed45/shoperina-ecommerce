/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.freepik.com",
      "images.pexels.com",
      "cdn.dummyjson.com",
      "i.ibb.co",
    ],
  },
};

export default nextConfig;
